{% extends template_from_string(data.website_layout) %}

{% block css %}

{% endblock %}

{% block page %}

    <!-- Section: inner-header -->
    <section class="inner-header divider parallax layer-overlay overlay-dark-5" data-bg-img="{{ img(field_render('subheader', 'page@'~data.page.id).path) }}">
        <div class="container pt-100 pb-50">
            <!-- Section Content -->
            <div class="section-content pt-100">
                <div class="row">
                    <div class="col-md-12">
                        <h3 class="title text-white">
                            {% if data.page.type == 'dynamic' %}
                                {{ data.route_params.slug | capitalize | replace({'-':' '}) }}
                            {% else %}
                                {{ data.page.title }}
                            {% endif %}
                        </h3>
                    </div>
                </div>
            </div>
        </div>
    </section>


    <!-- Section: Have Any Question -->
    <section class="divider">
        <div class="container pt-60 pb-60">
            <div class="section-content">
                <div class="row">
                    <div class="col-sm-12 col-md-4">
                        <div class="contact-info text-center">
                            <i class="fa fa-phone font-36 mb-10 text-theme-colored"></i>
                            <h4>Nous appeler</h4>
                            <h6 class="text-gray">Tél: {{ account.phone }}</h6>
                        </div>
                    </div>
                    <div class="col-sm-12 col-md-4">
                        <div class="contact-info text-center">
                            <i class="fa fa-map-marker font-36 mb-10 text-theme-colored"></i>
                            <h4>Adresse</h4>
                            <h6 class="text-gray">{{ address.address }}<br/> {{ address.postal_code }} {{ address.city }}<br/> {{ address.country }}</h6>
                        </div>
                    </div>
                    <div class="col-sm-12 col-md-4">
                        <div class="contact-info text-center">
                            <i class="fa fa-envelope font-36 mb-10 text-theme-colored"></i>
                            <h4>E-mail</h4>
                            <h6 class="text-gray">{{ account.email }}</h6>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!--======= Section Map ===== -->
    <section>
        <div class="container-fluid pt-0">
            <div class="row">
                <div id="gmap" style="height:400px"></div>
            </div>
        </div>
    </section>
    <!-- end main-content -->

    <section class="bg-theme-colored">
        <div class="container pt-30 pb-30">
            <div class="section-content">
                <div class="row">
                    <div class="col-md-8">
                        <h3 class="font-weight-400 text-white font-raleway letter-space-2">Not every woman has time to go to a salon and have her hair blow-dried every day.</h3>
                    </div>
                    <div class="col-md-4">
                        <a class="btn btn-dark mt-30 p-10 pl-20 pr-20 text-white btn-flat no-border" data-toggle="modal" data-target="#BSParentModal" href="{{ booking_link }}" class="bs-modal-ajax-load" >Réserver</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block js %}
    <script src="https://maps.googleapis.com/maps/api/js?v=3&key=AIzaSyArNGSSl-KPS23l24EilPNQhfmEw_V8BtI&libraries=places&region=FR"></script>
    <script>
        $(document).ready(function () {
            var latlng = new google.maps.LatLng({{ address.latitude }}, {{ address.longitude }});
            var map = new google.maps.Map(document.getElementById('gmap'), {
                zoom: 13,
                center: latlng
            });
            new google.maps.Marker({
                position: latlng,
                map: map
            });
        })
    </script>
{% endblock %}